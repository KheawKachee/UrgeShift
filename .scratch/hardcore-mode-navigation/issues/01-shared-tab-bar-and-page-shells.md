Status: ready-for-agent

## What to build

Add a shared bottom tab bar across the top-level app routes and remove page-specific route-navigation clusters. The tab bar should expose `Shift`, `ภูติ`, `Preview`, `Plans`, and `Progress`, and should visually unify the current routes into one product flow.

## Acceptance criteria

- [ ] All top-level routes use one shared tab bar with the approved five destinations
- [ ] Existing backlink and cross-page route buttons are removed from page bodies and replaced by the tab bar
- [ ] The tab bar highlights the active route and fits the current UrgeShift visual tone on mobile and desktop

## Blocked by

None - can start immediately
